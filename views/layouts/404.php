<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 — Geprek Geh</title>
    <style>
        body { margin:0; font-family: 'DM Sans', system-ui, sans-serif; background: #14110C; color: #f6e3c7; display: flex; align-items: center; justify-content: center; min-height: 100vh; }
        .err { text-align: center; padding: 2rem; max-width: 420px; }
        .err h1 { font-size: 5rem; margin: 0; opacity: .6; letter-spacing: -.04em; }
        .err p { font-size: 1.1rem; opacity: .85; margin: 1rem 0 1.5rem; }
        .err-links { display: flex; gap: .75rem; justify-content: center; flex-wrap: wrap; }
        .err-links a { display: inline-block; padding: .7rem 1.6rem; border-radius: 999px; text-decoration: none; font-weight: 600; transition: opacity .2s; }
        .err-links .primary { background: #d43e1b; color: #fff; }
        .err-links .ghost { border: 1.5px solid rgba(246,227,199,.25); color: #f6e3c7; }
        .err-links a:hover { opacity: .85; }
    </style>
</head>
<body>
    <div class="err">
        <h1>404</h1>
        <p>Halaman yang kamu cari tidak ditemukan atau sudah dipindahkan.</p>
        <div class="err-links">
            <a href="/geprek-geh/" class="primary">Beranda</a>
            <a href="/geprek-geh/products" class="ghost">Lihat Menu</a>
        </div>
    </div>
</body>
</html>
